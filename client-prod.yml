build:
  version: 3.0

server:
  port : 8090

info:
  name: "vishal"
  
contact:
  services: "client-prod configuration for github repo /refresh 00"
eureka:
  instance:
    preferIpAddress : true
  client:
    fetchRegistry: true
    registerWithEureka : true
    serviceUrl: 
      defaultZone: http://localhost:8070/eureka/
